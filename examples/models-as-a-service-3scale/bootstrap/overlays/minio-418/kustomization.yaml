apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-gitops

resources:
  - ../../base

patches:
  - path: external-secrets-operator-patch.yaml
    target:
      group: argoproj.io
      version: v1alpha1
      kind: ApplicationSet
