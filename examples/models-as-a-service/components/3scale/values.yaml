# Default values for 3scale-operator

# namespace: The namespace to deploy the 3scale operator
namespace: admachad-3scale-test
# channel: The 3scale operator channel to subscribe to
channel: threescale-2.15
